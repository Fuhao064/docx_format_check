# Web 框架（Flask + Socket.IO 实时事件）
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6

# 请求校验与 LLM 客户端
pydantic
openai>=1.0.0

# 文档处理
PyMuPDF>=1.23.0
python-docx>=1.1.0

# Windows 平台：Word COM 高保真引擎所需。
# 未安装时引擎会自动降级到 python-docx（macOS / Linux 无需此包）。
pywin32>=306; sys_platform == "win32"

# 本地推理可选依赖
# llama-cpp-python>=0.2.0
# optimum[onnxruntime]>=1.15.0
# transformers>=4.35.0
# torch>=2.0.0
# onnx>=1.15.0
# onnxruntime>=1.16.0
